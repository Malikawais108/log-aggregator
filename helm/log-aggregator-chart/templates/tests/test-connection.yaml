apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "log-aggregator.fullname" . }}-test-connection"
  labels:
    {{- include "log-aggregator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "log-aggregator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
